sed -i 's/saveStoredRepository({ userAccounts: updatedUsers });/const toUnban = [account.id, account.username, account.email].filter(Boolean).map(k => String(k).trim().toLowerCase());\n    const updatedDeletedIds = (repo.deletedUserIds || []).filter(id => !toUnban.includes(String(id).trim().toLowerCase()));\n    saveStoredRepository({ userAccounts: updatedUsers, deletedUserIds: updatedDeletedIds });/g' server.ts
